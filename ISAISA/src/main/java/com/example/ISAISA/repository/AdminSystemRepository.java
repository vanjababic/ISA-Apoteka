package com.example.ISAISA.repository;

import com.example.ISAISA.model.AdminSystem;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AdminSystemRepository extends JpaRepository<AdminSystem, Integer> {
}
